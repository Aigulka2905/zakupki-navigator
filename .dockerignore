# ── Зависимости и артефакты сборки ──────────────────────────
node_modules
dist
dist-ssr
build
coverage
*.local

# ── VCS / IDE ───────────────────────────────────────────────
.git
.gitignore
.gitattributes
.idea
.vscode
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# ── Docker / CI (не нужны внутри образа) ────────────────────
# ВНИМАНИЕ: каталог docker/ НЕ игнорируем целиком — из него COPY-ятся
# entrypoint.sh. Исключаем только то, что точно не нужно в контексте.
docker/dev/docker-compose.yml
docker/prod/docker-compose.prod.yml
docker/env
docker/scripts
Makefile
.dockerignore

# ── Логи и временные файлы ──────────────────────────────────
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
.DS_Store

# ── Окружение и секреты ─────────────────────────────────────
.env
.env.*
!.env.example

# ── Альтернативный lock-файл (в образе используем npm) ──────
bun.lockb

# ── Документация ────────────────────────────────────────────
README.md
CLAUDE.md
*.md
